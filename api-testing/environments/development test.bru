vars {
  baseUrl: http://localhost:3000
  apiUrl: {{baseUrl}}/api
}
vars:secret [
  testEmail,
  testPassword,
  accessToken,
  refreshToken,
  testAlbumId,
  testAccessToken,
  testAccessLinkId,
  testPhotoId
]
